{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/classify-number",
      "dest": "/api/classify-number.ts"
    }
  ]
}
